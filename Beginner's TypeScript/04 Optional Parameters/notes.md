Instead of passing an object, we are now passing two function parameters. 

Optional works the same was as it did in the previous lesson. 

The required argument should come before the optional argument. 